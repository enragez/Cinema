@page
@model Cinema.Pages.FilmSessions.BookConfirmModel

@{
    ViewData["Title"] = "Подтверждение бронирования";
}

<h1>Забронировать билет</h1>

<h3>Вы действительно хотите забронировать этот билет?</h3>
<div>
    <dl class="row">
        <dt class="col-sm-2">
            Фильм: @Model.FilmSession.Film
            Время: @Model.FilmSession.Date
            Зал: @Model.FilmSession.Hall
            Цена: @Model.FilmSession.Price
            Ряд: @Model.Row
            Место: @Model.Column
        </dt>
    </dl>
    
    <form method="post">
        <input type="hidden" asp-for="FilmSession.Id" />
        <input type="submit" value="Забронировать" class="btn btn-primary" /> |
        <a asp-page="./Book" asp-route-id="@Model.FilmSession.Id">Назад к схеме зала</a>
    </form>
</div>