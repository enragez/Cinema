@page
@model Cinema.Pages.MyTickets.CancelBookModel

@{
    ViewData["Title"] = "Отмена бронирования";
}

<h1>Отменить бронирование</h1>

<h3>Вы действительно хотите отменить бронирование этого билета?</h3>
<div>
    <dl class="row">
        <dt class="col-sm-2">
            Фильм: @Model.Ticket.Film
            Время: @Model.Ticket.Date
            Зал: @Model.Ticket.Hall
            Ряд: @Model.Ticket.Row
            Место: @Model.Ticket.Column
        </dt>
    </dl>
    
    <form method="post">
        <input type="hidden" asp-for="Ticket.Id" />
        <input type="submit" value="Отменить" class="btn btn-danger" /> |
        <a asp-page="./Index">Назад к списку</a>
    </form>
</div>